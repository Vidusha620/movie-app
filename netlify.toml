[build]
  command = "CI=false npm run build"
  publish = "build"
  environment = { NODE_VERSION = "18" }

# Handle Single Page Application routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
